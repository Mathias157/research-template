# Configuration for the analysis pipeline.
# Add your own keys; the demo uses `slope` and `x0`.

slope: 4
x0: 5
white_colourmap: batlowW
dark_colourmap: batlowW_r
dark_plots: false

# Archive rule (`snakemake archive`) settings.
push:
    from: build
    to: archive
    exclude-paths:
