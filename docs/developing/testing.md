# Testing

The test suite uses [pytest](https://docs.pytest.org/) and lives in the `tests/` directory. Testing is included in the Snakemake pipeline by default, or can be run standalone with:

```bash
pixi shell
snakemake test
```


